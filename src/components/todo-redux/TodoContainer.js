import TodoForm from "./TodoForm";
import TodoList from "./TodoList";

const TodoContainer = () => {
  return (
    <>
      <TodoForm />
      <TodoList />
    </>
  );
};

export default TodoContainer;
